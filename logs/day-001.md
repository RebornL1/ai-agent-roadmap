# Day 1 Learning Log

## 今日完成

- Created `projects/dba-agent-workbench`.
- Initialized a Next.js + TypeScript + Tailwind CSS app.
- Built a three-pane AI Agent Workbench mock UI:
  - Left: DBA incident case list
  - Center: chat workspace
  - Right: agent timeline and tool-call panel
- Used mock data only.
- Added project README.
- Passed lint and production build checks.

## 遇到的问题

- Initial dependency installation timed out because npm registry downloads were slow.
- pnpm 11 blocked native dependency build scripts by default.
- Fixed the install by allowing the required `sharp` and `unrs-resolver` builds in `pnpm-workspace.yaml`.

## 明天计划

- Add fake streaming output.
- Add simple client-side message state.
- Add stop / retry / regenerate UI states.
- Keep all model calls mocked.

## 面试可讲点

- The first project turns real DBA incident response into an AI Agent product surface.
- The UI separates conversation, execution state, and tool-call audit trail.
- The design keeps read-only diagnostics and approval states visible from Day 1.
- This is the foundation for later RAG, MCP, LangGraph, and Human Approval work.

## Git commit message 建议

```text
feat: initialize dba agent workbench
```
